# Boardbook marketing site

Static Astro page for the billboard dashboard. From `site/`:

```
npm install
npm run dev
npm run build
npm run preview
```

Product name and contact live in `src/config.ts`.
